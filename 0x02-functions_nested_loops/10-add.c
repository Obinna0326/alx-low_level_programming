#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: added to b
 * @b: added to a
 * Return: The sum of a and b are to be returned
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
